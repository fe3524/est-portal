<body class="sb-nav-fixed">
    <%= render 'navbar'%>
    <%= render 'sidenav-menu'%>
    <%= render 'sidenav-footer'%>
    <%= render 'main-top'%>

    <!-- メインコンテンツ -->
    <h1>アカウントの作成見積一覧表示</h1>
    <%= link_to "PDFを表示", post_pdf_index_path(format: "pdf") %>

    <script src="app/javascript/mypege_scripts.js"></script>
</body>

<%= render 'footer'%>
